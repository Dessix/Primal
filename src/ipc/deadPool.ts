
export class DeadPool {
  //Register to a global, volatile listing that expires at end of tick or upon being consumed
  public static registerPosthumous(creepName: string, creepMemory: CreepMemory) {
    throw new Error("Not Implemented!");
  }
}

